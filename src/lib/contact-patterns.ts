/**
 * 連絡先（電話番号）検出の共通部品。
 *
 * 電話番号の「区切り文字クラス」を単一ソース化する。この区切り集合が
 * 3 ファイル（ai-privacy.maskContactInfo / ai-knowledge/pii-sanitizer /
 * community/ng-words）で個別に定義され、区切りの追加が横展開されず sibling に
 * 漏れが残る事故が繰り返された（Day43 / Day50 / Day50-PM で個別に後追い修正）。
 * ここに集約し、区切りの追加は本ファイル 1 箇所で全経路に効くようにする。
 *
 * 含める区切り: ハイフン(-) 空白(\s) 長音(ー) 各種ダッシュ(− ‐ ―) ドット(.) 中黒(・) アンダースコア(_)。
 * ー(U+30FC)・−(U+2212)・‐(U+2010)・―(U+2015) は NFKC 正規化でも '-' にならないため個別に含める。
 *
 * 用途が各所で異なるため（置換 vs ラベル検出、10桁ガード有無、NFKC 正規化有無）、
 * 正規表現本体は各モジュールが組む。共通化するのは「毎回ズレていた区切り集合」のみ。
 */
export const PHONE_SEP_CLASS = '[-\\sー−‐―.・_]';
